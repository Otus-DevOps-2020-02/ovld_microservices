version: '3.3'
services:
  ui:
    command: ${UI_CMD}
    volumes: ${UI_SOURCE_CODE}:/app
  post:
    command: ${POST_PYTHON_SCRIPT_PATH}
    volumes: ${POST_SOURCE_CODE}:/app
  comment:
    command: ${COMMENT_CMD}
    volumes: ${COMMENT_SOURCE_CODE}:/app
